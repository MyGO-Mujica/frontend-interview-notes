# 场景题

### 常见面试问题

#### 1. 场景题，现在需要你用 react 写一个搜索框，搜索结果是一个列表，请说说你的实现思路

> 实现思路是：维护三个状态，搜索关键词 `query`、结果列表 `list`、加载状态 `loading`。
输入框 onChange 时更新 query，同时触发搜索，但搜索要加防抖，避免每次输入都请求接口，一般 300ms。
有一个细节要处理是输入为空时直接清空列表不发请求。
列表渲染用 key 绑定唯一 id，请求中展示 loading 状态提升体验。